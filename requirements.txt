starlette>=0.40.0
uvicorn>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.9
httpx>=0.27.0
# websockets is the upstream client our reverse proxy uses to connect to
# hermes's /api/pty, /api/ws, /api/events endpoints. httpx does not speak
# WebSockets, so we need a dedicated client library.
websockets>=12.0
# openpyxl lets the receipt-coding skill emit a real .xlsx approval sheet
# (frozen header, currency formatting, flagged rows shaded) instead of a bare
# CSV. build_approval_sheet.py degrades to CSV when this import fails, so a
# missing wheel cannot lose a coded batch, but the highlighted review rows are
# the point of the artifact in front of a bookkeeper. Confirmed absent from the
# image on 2026-07-28: hermes-agent's extras do not pull it in.
openpyxl>=3.1.0

# cryptography provides Fernet (AES-128-CBC + HMAC-SHA256), used by
# vault.py to decrypt portal credentials pulled from Supabase. The key
# lives only in the Railway variable BAILEY_VAULT_KEY; the database holds
# ciphertext. Confirmed absent from the image on 2026-08-31.
cryptography>=43.0.0
